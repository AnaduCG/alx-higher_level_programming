def raise_exception_msg(message=""):
    """raise_exception_msg - function that raises a name
    exception with a message.

        Args:
            message (str, optional): message to be printed on error"".
    """
    raise NameError(message)
